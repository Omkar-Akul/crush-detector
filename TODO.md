- [ ] Implement theme variable system (index.css): dark default + light overrides via data-theme + prefers-color-scheme.
- [ ] Persist theme selection (App.jsx): read OS preference on first load, override with localStorage, apply to root element, add header-only toggle.
- [ ] Update light CSS hard-codes (App.css): swap key color hard-codes to variables (backgrounds/text/borders) without changing layout.
- [ ] Run frontend build/tests and verify no UI breakage (desktop + mobile).

